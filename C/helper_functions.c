#include <stdio.h>
#include <stdlib.h>

long int convertToLongInt(char* value){
    return atol(value);
}

long int convertToInt(char* value){
    return atol(value);
}

float convertToFloat(char* value){
    return atof(value);
}